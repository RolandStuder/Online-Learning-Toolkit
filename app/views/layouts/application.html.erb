<!DOCTYPE html>
<html>
<head>
  <title>Peer Review</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<div class="container">
<header>
	<%= link_to "Home", '/' %>
	<%= link_to "New", '/peer_reviews/new' %>
	<%= link_to "Peer Reviews", '/peer_reviews' %>
	
</header>
	
	<% flash.each do |key, msg| %>
	    <%= content_tag :div, msg, :class => [key, " message"], :id => "notice_ #{key}" %>
	    <%= content_tag :script, :type => "text/javascript" do -%>
	        setTimeout("new Effect.Fade('notice_<%= key %>');", 15000);
	    <% end %>
	<% end %>
	
	
<%= yield %>

<footer>
	developed by <a href="mailto:roland.studer@psy.unibe.ch">roland.studer@psy.unibe.ch</a>, for <a href="http://www.edulap.ch">edulap</a>, <a href="http://www.unibe.ch">unibe</a>
</footer>
</div>

</body>
</html>
