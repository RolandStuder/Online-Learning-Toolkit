<%= form_for(@feedback) do |f| %>
  <% if @feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback.errors.count, "error") %> prohibited this peer_review_feedback from being saved:</h2>

      <ul>
      <% @feedback.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h2><%= f.label "Your feedback for this solution" %></h2>
    <%= f.text_area :text, :class => "mceEditor" %>
  </div>
  <div class="actions">
    <%= f.submit "Save Feedback", :class => "primary-button breathe" %>
  </div>

<% end %>
