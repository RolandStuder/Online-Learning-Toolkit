<h1>Give Feedback for the solution</h1>

<h2>Solution for task:<br><%= @peer_review.title %></h2>

<%= render :inline => @solution.text %>
<%= render 'form' %>

<%= link_to 'Back', peer_review_assignment_path(@assignment) %>
